# 中台-mode reference Helm chart for spring-ai-ascend.
#
# product_claim: PC-002 (Two Chinese-enterprise topologies plus sovereign hardware,
#                        one artifact set — sub-dimension middle_office_topology_中台)
# target_personas: Persona-A (Platform Team Lead, 中台 buyer), Persona-D (SRE)
# v1_0_ship_blocker: true
# authority: ADR-0156
#
# This chart deploys the 中台 pattern: the central platform team operates the
# runtime + bus + middleware + audit + observability stack. Business centers
# push agent definitions into the runtime via the skill-bundle artifact
# pipeline (out of scope for THIS chart; see deploy/skill-bundle-pipeline/
# in a follow-up wave).

apiVersion: v2
name: middle-office-reference
description: |
  中台-mode reference deployment for spring-ai-ascend. The central platform
  team operates this chart; business centers consume the runtime via the
  HTTP API + skill-bundle push pipeline. Sized for Kunpeng 920 ARM64 +
  Ascend 310 NPU per docs/perf/v1.0-baselines.yaml#reference_hardware.
type: application
version: 1.0.0-finance
appVersion: "1.0.0-finance"
kubeVersion: ">= 1.27.0"
keywords:
  - spring-ai-ascend
  - middle-office
  - 中台
  - financial-vertical
  - kunpeng
  - ascend
  - sovereign
maintainers:
  - name: spring-ai-ascend-platform-team
annotations:
  product_claim: PC-002
  target_personas: "Persona-A,Persona-D"
  authority: "ADR-0156"
  v1_0_ship_blocker: "true"
  status: "design_only"
  maturity: "L0 — scaffold, not deployment-tested. Promotes to L1 when the impl-mode wave wires the Postgres RLS + audit-trail + IAM bridge + financial sandbox policy templates and runs a soak on Kunpeng+Ascend hardware."
