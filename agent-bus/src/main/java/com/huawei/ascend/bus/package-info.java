/**
 * agent-bus — Bus & State Hub plane (skeleton).
 *
 * <p>Empty placeholder package. Runtime implementations of the
 * WorkflowIntermediary, Mailbox, AdmissionDecision, BackpressureSignal,
 * SleepDeclaration, WakeupPulse, and TickEngine SPIs land in W2 per
 * ADR-0050. The contract surface for the three-track channel isolation
 * already ships in {@code docs/governance/bus-channels.yaml} (Rule 35 /
 * P-E).
 *
 * <p>Authority: ADR-0050, Layer-0 principles P-E + P-I, Rule 35.
 */
package com.huawei.ascend.bus;
