# Delivery Record: spring-ai-fin → spring-ai-ascend rename

**SHA**: c0cf562 (content changes) + 9b5368a (directory/file renames)
**Date**: 2026-05-12
**Type**: `direct_reviewed_content` — full-identity project rename
**Author**: Chao Xing

## What this delivery documents

Project identity rename from `spring-ai-fin` to `spring-ai-ascend`. The rename spans two commits:
- `9b5368a` — directory and file renames via `git mv` (176 files, rename-only)
- `c0cf562` — content rewrites: package declarations, imports, groupId, artifactIds, AutoConfiguration FQCNs, OpenAPI snapshot, governance manifests, docs (189 files, 798 insertions / 795 deletions)

Together they constitute the complete atomic identity rename. Every hot-path file (per Rule 8 definition) is touched, invalidating all prior `latest_delivery_valid_sha` entries in `docs/governance/architecture-status.yaml`.

## Scope

| Surface | Change |
|---------|--------|
| GitHub repo | `chaosxingxc-orion/spring-ai-fin` → `chaosxingxc-orion/spring-ai-ascend` |
| Maven groupId | `fin.springai` → `com.huawei.ascend` |
| Maven parent | `spring-ai-fin-parent` → `spring-ai-ascend-parent` |
| 11 starter modules | `spring-ai-fin-*` → `spring-ai-ascend-*` |
| Java root package | `fin.springai.*` → `com.huawei.ascend.*` |
| Helm chart | `ops/helm/spring-ai-fin/` → `ops/helm/spring-ai-ascend/` |
| Historical docs | moved to `docs/archive/spring-ai-fin/` (byte-for-byte, frozen) |
| Commit 9b5368a | 176 files renamed |
| Commit c0cf562 | 189 files, 798 insertions / 795 deletions (content) |

## Architecture-sync gate status at c0cf562

| Check | Result |
|-------|--------|
| Identity strings removed from live tree | PASS — grep returns zero hits outside archive |
| Java package directories renamed | PASS — all `fin/springai` paths → `ascend/springai` |
| Maven coordinates updated | PASS — groupId and artifactIds in all 14 module poms |
| AutoConfiguration.imports updated | PASS — 11 starters updated atomically |
| OpenAPI pinned snapshot updated | PASS — `info.title` and `info.contact.name` |
| Governance manifest delivery pointers | PASS — archive paths updated |
| `latest_delivery_valid_sha` reset | PASS — all rows set to null with comment |

## Rule 8 eligibility

Not applicable to this delivery. This commit performs an identity rename, not a behavioral change. Rule 8 operator-shape gate (real-dependency N≥3 sequential runs) is a W4 deliverable per `docs/plans/engineering-plan-W0-W4.md`. Gate state remains `fail_closed_needs_real_flow`.

## Collaborator migration steps

See `ops/runbooks/rename-spring-ai-fin-to-ascend.md` for the one-time actions collaborators must take (remote URL update + Maven cache purge + rebuild).
