/**
 * agent-middleware — runtime-owned cross-cutting middleware (skeleton root).
 *
 * <p>Placeholder root package. SPI types live in
 * {@link com.huawei.ascend.middleware.spi} (HookPoint, RuntimeMiddleware,
 * HookContext, HookOutcome). The dispatcher implementation
 * ({@code HookDispatcher}) lives directly under this package.
 *
 * <p>Authority: ADR-0073, Layer-0 principle P-M, Rule 45.
 */
package com.huawei.ascend.middleware;
