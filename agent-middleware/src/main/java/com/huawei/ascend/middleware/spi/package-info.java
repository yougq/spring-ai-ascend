/**
 * agent-middleware SPI — HookPoint / RuntimeMiddleware / HookContext / HookOutcome.
 *
 * <p>SPI-pure package per CLAUDE.md Rule 32: imports restricted to
 * {@code java.*} and own spi siblings. Enforced by
 * {@code SpiPurityGeneralizedArchTest} (E48).
 *
 * <p>Authority: ADR-0073, Layer-0 principle P-M, Rule 45.
 */
package com.huawei.ascend.middleware.spi;
