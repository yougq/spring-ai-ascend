# Spring AI Ascend - agent-runtime-a2a-openjiuwen-e2e sample configuration.
# Copy to `.env` (gitignored) and fill in your model service, then run:
#   bash scripts/test-e2e.sh .env            (Linux/WSL/macOS)
#   ./scripts/test-e2e.ps1 -EnvFile .env     (Windows PowerShell)
#
# IMPORTANT: the real-LLM branch of OpenJiuwenReactAgentA2aE2eTest only runs
# when SAA_SAMPLE_LLM_API_KEY is non-blank. Without it, JUnit assumeTrue() skips
# the real model call after the agent-card check.

# Any non-blank value enables the real-LLM branch. Use your provider's key/token.
SAA_SAMPLE_LLM_API_KEY=
# openai = OpenAI-compatible /v1 (OpenAI, LiteLLM, Ollama /v1, vLLM, Azure-OpenAI-compat, ...).
SAA_SAMPLE_OPENJIUWEN_MODEL_PROVIDER=openai
# Base URL of an OpenAI-compatible endpoint.
SAA_SAMPLE_OPENJIUWEN_API_BASE=http://localhost:4000/v1
# Model name served by that endpoint.
SAA_SAMPLE_LLM_MODEL=gpt-5.4-mini
# TLS verification (false for local http endpoints; true for cloud https).
SAA_SAMPLE_OPENJIUWEN_SSL_VERIFY=false
# Checkpointer backend for openJiuwen conversation state: in-memory or redis.
SAA_SAMPLE_OPENJIUWEN_CHECKPOINTER=in-memory
# Used only when SAA_SAMPLE_OPENJIUWEN_CHECKPOINTER=redis.
SAA_SAMPLE_OPENJIUWEN_REDIS_URL=redis://localhost:6379
