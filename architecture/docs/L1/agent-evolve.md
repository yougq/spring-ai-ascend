# agent-evolve — L1 Narrative

**Authority:** ADR-0147 (Structurizr Workspace Authority); narrative authored under W2 of the migration.

agent-evolve is the evolution-plane boundary on the dedicated Evolution plane. Captures Run trajectories with `EvolutionExport` discrimination (`IN_SCOPE` / `OUT_OF_SCOPE` / `OPT_IN`) per Rule R-M.e. Sealed `RunEvent` hierarchy specified per ADR-0145.

W1 ships skeleton only; the ML/self-improvement pipeline lands in W2+.

## Cross-references

`agent-evolve/module-metadata.yaml`, `agent-evolve/ARCHITECTURE.md` (legacy).
